# خطة تطوير وتجهيز نظام Ahgzly POS للسوق المصري

بناءً على المراجعة الشاملة للكود المصدري للمشروع (بنية Clean Architecture، استخدام Drift، Bloc، GoRouter)، تبين أن المشروع يمتلك أساساً برمجياً ممتازاً، ولكنه يحتوي على ثغرات تقنية ومنطقية تمنع بيعه كنظام تجاري حقيقي للمطاعم في مصر. 

فيما يلي خطة تفصيلية للتحسين مقسمة إلى **Sprints** لضمان التنفيذ السريع والمنظم.

> [!WARNING]
> **الثغرة الأخطر حالياً:** نظام الطباعة يعتمد على تحويل الـ UI إلى صورة (Screenshot) ثم إلى PDF للطباعة. هذا الأسلوب بطيء جداً، يستهلك الذاكرة، ويفشل مع طابعات الإيصالات الحرارية (Epson, Xprinter) الشائعة في مصر. يجب التحول الفوري إلى نظام الأوامر المباشرة (ESC/POS).

> [!CAUTION]
> **الثغرة الأمنية:** نظام التراخيص (Licensing) يعتمد على حقل `isActivated` داخل قاعدة بيانات SQLite المحلية. يمكن لأي شخص تجاوز الترخيص بمجرد استبدال ملف قاعدة البيانات. يجب ربط الترخيص بالمعرف الصلب للجهاز (Hardware ID / MAC Address).

---

## 📅 Sprint 1: الثغرات الحرجة والبنية التحتية الأساسية
**المدة المقترحة:** أسبوع واحد
**الهدف:** معالجة المشاكل التي تمنع تشغيل النظام واستقراره في بيئة عمل حقيقية (مثل سرعة الطباعة وأمان الترخيص).

### 1. نظام الطباعة (Printing Engine)
- **المشكلة:** استخدام `screenshot` و `pdf` للطباعة.
- **الحل:** إعادة كتابة `PrinterService` ليستخدم حزمة `esc_pos_utils_plus` مع `network` أو `usb` لإرسال أوامر ESC/POS المباشرة للطابعة.
- **[MODIFY]** `lib/core/services/printer_service.dart`

### 2. الحماية والتراخيص (Security & Licensing)
- **المشكلة:** التراخيص تخزن كقيمة Boolean عادية في قاعدة البيانات.
- **الحل:** 
  - جلب المعرف الفريد للجهاز (Hardware ID) باستخدام `device_info_plus`.
  - تشفير مفتاح التفعيل (AES Encryption) بحيث يكون صالحاً لهذا الجهاز فقط.
- **[MODIFY]** `lib/features/license/domain/` & `data/`

### 3. النسخ الاحتياطي التلقائي (Automated Backups)
- **المشكلة:** النسخ الاحتياطي يعتمد على التدخل اليدوي (FilePicker).
- **الحل:** إنشاء `CronJob` أو Isolate يعمل في الخلفية لأخذ نسخة احتياطية من ملف `pos_sys_drift.db` يومياً عند إغلاق الوردية وحفظه في مجلد محدد أو سحابة.
- **[MODIFY]** `lib/core/services/backup_service.dart`

---

## 📅 Sprint 2: دورة العمل والورديات (Shift & Workflow)
**المدة المقترحة:** أسبوع واحد
**الهدف:** إحكام الرقابة المالية ومنع تسرب الإيرادات (أهم متطلب لأصحاب المطاعم في مصر).

### 1. إجبار إغلاق الوردية (Force Shift Closure)
- **المشكلة:** يمكن إغلاق البرنامج (Exit) أو تسجيل الخروج دون إغلاق الوردية الحالية `pos_screen.dart`.
- **الحل:** منع الخروج من التطبيق إذا كانت هناك وردية مفتوحة للمستخدم، وإظهار نافذة (يجب إغلاق الوردية أولاً وتسليم العهدة).
- **[MODIFY]** `lib/features/pos/presentation/screens/pos_screen.dart`

### 2. صلاحيات المستخدمين المتقدمة (Advanced Permissions)
- **المشكلة:** الصلاحيات حالياً مقتصرة غالباً على `isAdmin` بشكل عام.
- **الحل:** إضافة صلاحيات دقيقة: (حذف صنف من الطلب بعد إضافته، عمل خصم، إلغاء فاتورة، فتح الدرج بدون بيع). يتطلب ذلك إدخال رمز المدير للموافقة على هذه الحركات المحددة.
- **[MODIFY]** `lib/features/auth/domain/` و شاشات الـ POS.

### 3. الضرائب والخدمة (Taxes & Service)
- **المشكلة:** حسابات الضريبة متوفرة في الجداول، ولكن يجب التأكد من توافقها مع قانون القيمة المضافة المصري (VAT 14%).
- **الحل:** فصل الضريبة عن السعر (إما السعر شامل الضريبة أو يضاف عليها) وإظهار الرقم الضريبي للمطعم بشكل إجباري على الفاتورة المطبوعة.

---

## 📅 Sprint 3: تجربة المستخدم والسرعة (UI/UX & Speed)
**المدة المقترحة:** 3 إلى 5 أيام
**الهدف:** تسريع عملية إدخال الطلبات للمنافسة مع الأنظمة التقليدية.

### 1. اختصارات لوحة المفاتيح (Keyboard Shortcuts)
- **المشكلة:** الاعتماد الكامل على الماوس/اللمس.
- **الحل:** إضافة اختصارات للشاشة الرئيسية (مثل: `F12` للدفع، `F5` لفتح درج النقود، `Esc` للإلغاء) باستخدام `HardwareKeyboardListener`.
- **[MODIFY]** `lib/features/pos/presentation/screens/pos_screen.dart`

### 2. تحسينات واجهة الكاشير
- **الحل:** 
  - دعم قراءة الباركود (Barcode Scanner) مباشرة من الـ Keyboard Buffer دون الحاجة للوقوف في حقل إدخال (TextFormField) محدد.
  - إظهار التنبيهات (Snackbars) بطريقة لا تعيق الرؤية، وتختفي بسرعة.

---

## 💡 Open Questions (أسئلة للنقاش قبل البدء)

> [!NOTE]
> يرجى الإجابة على هذه الأسئلة لتحديد مسار التنفيذ بدقة:
> 1. **الطباعة:** هل تريد دعم طابعات الـ USB فقط في المرحلة الأولى أم طابعات الشبكة (Network/Ethernet) أيضاً؟ (في مصر طابعات الشبكة شائعة جداً في المطبخ).
> 2. **التراخيص:** هل تفضل أن يتم التحقق من الترخيص أوفلاين (بناءً على تشفير معتمد على هاردوير الجهاز) أم من خلال API سحابي يتصل بالإنترنت؟
> 3. **النسخ الاحتياطي:** هل نكتفي بعمل نسخ احتياطي تلقائي مشفر على القرص الصلب المحلي، أم نربطه بخدمة مثل Google Drive؟

## ✅ خطة التحقق (Verification Plan)
1. **الطباعة:** تجربة طباعة إيصال حقيقي مكون من 20 صنف وقياس سرعة خروج الورقة (يجب أن تكون لحظية).
2. **الأمان:** محاولة نقل قاعدة البيانات لجهاز آخر للتأكد من فشل التفعيل.
3. **الورديات:** محاولة إغلاق التطبيق بالقوة ومراجعة صحة حسابات العهدة.

---
**في انتظار موافقتك على الخطة أو إجابتك على الأسئلة لنبدأ التنفيذ مباشرة بـ Sprint 1!**
